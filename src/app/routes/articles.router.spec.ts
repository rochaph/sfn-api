import articles from "./articles.router";

describe("articles.ts", () => {
  test("Deve retornar uma lista de artigos quando a request for válida", () => {
    return expect(true).toEqual(true);
  });

  // test('Deve retornar um artigo quando a request for válida', () => {});

  // test('Deve adicionar um artigo quando a request for válida', () => {});

  // test('Deve atualizar um artigo quando a request for válida', () => {});

  // test('Deve deletar um artigo quando a request for válida', () => {});
});
